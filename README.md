### Запуск проекта

### Требования
- Docker
- Docker Compose

### Запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/slyjewbro/qna-api.git
bash
cd qna-api
docker-compose up --build
Приложение будет доступно по адресу: http://localhost:8080

Примеры запросов
Создание вопроса

bash
curl -X POST http://localhost:8080/questions \
    -H "Content-Type: application/json" \
    -d '{"text": "Как работает Go?"}'
Добавление ответа

bash
curl -X POST http://localhost:8080/questions/1/answers \
    -H "Content-Type: application/json" \
    -d '{
    "user_id": "user-123",
    "text": "Go - это компилируемый язык программирования"
}'
Миграции
Миграции выполняются автоматически при запуске через Docker Compose.

Тестирование
Для запуска тестов:

text

**Что было убрано:**
- Дублирующиеся команды `git clone`
- Лишние блоки с конфликтными маркерами (`<<<<<<<`, `==========`, `>>>>>>>`)
- Неправильное написание "Ииграции" → исправлено на "Миграции"
- Лишний открывающий тег `bash` без закрывающего
- Неполная команда для тестирования (оставлено как есть, поскольку в исходном тексте она не была завершена)

**Что оставлено:**
- Чистая структура документации
- Правильные команды для клонирования и запуска
- Примеры API запросов
- Информация о миграциях и тестировании